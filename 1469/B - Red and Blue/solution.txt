def RedAndBlue():    tc = int(input())     for _ in range(tc):        n = int(input())        reds = list(map(int, input().split()))        m = int(input())        blues = list(map(int, input().split()))         p_reds = [0] * (n + 1)        p_blues = [0] * (m + 1)                max_pReds = 0        for i in range(1,n+1):            p_reds[i] =  p_reds[i-1] + reds[i-1]            max_pReds = max(p_reds[i],max_pReds)                max_pBlues = 0        for i in range(1, m + 1):            p_blues[i] = p_blues[i-1] + blues[i-1]            max_pBlues = max(p_blues[i],max_pBlues)            print(max(0, max_pBlues + max_pReds)) RedAndBlue()