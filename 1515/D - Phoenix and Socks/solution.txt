from collections import Counter def phoneixAndSocks():    tc = int(input())        for _ in range(tc):        n, l_total, r_total = map(int, input().split())        colors = list(map(int, input().split()))                left_part = colors[:l_total]        right_part = colors[l_total:]                left_cnt = Counter(left_part)        right_cnt = Counter(right_part)                for color in left_cnt:            if color in right_cnt:                matches = min(left_cnt[color], right_cnt[color])                left_cnt[color] -= matches                right_cnt[color] -= matches                l_total -= matches                r_total -= matches                if r_total > l_total:            l_total, r_total = r_total, l_total            left_cnt, right_cnt = right_cnt, left_cnt                move_nums = (l_total - r_total) // 2         extra_pairs = 0        for color in left_cnt:            extra_pairs += left_cnt[color] // 2         ans = move_nums         if move_nums > extra_pairs:            ans += (move_nums - extra_pairs)         ans += r_total            print(ans)        phoneixAndSocks()