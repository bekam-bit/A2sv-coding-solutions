def BrokenKeyboard():    tc = int(input())     for _ in range(tc):        st = list(input())         left = 0         n = len(st)        res = []        seen = set()         while left < n:            right = left + 1                        while right < n and st[left] == st[right]:                right += 1                        leng = right - left             if leng % 2 != 0:                if st[left] not in seen:                    res.append(st[left])                    seen.add(st[left])                        left = right                    sorted_res = sorted(res)        print("".join(sorted_res)) if res else print("") BrokenKeyboard()